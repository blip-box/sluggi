extends:
  - "@commitlint/config-conventional"

rules:
  # Dependabot writes changelog links and compare URLs that exceed the
  # 100-character default and cannot be wrapped. Subject rules still apply.
  body-max-line-length: [0, "always", "Infinity"]
  footer-max-line-length: [0, "always", "Infinity"]
